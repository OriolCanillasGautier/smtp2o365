# Core SMTP server and client libraries
aiosmtpd>=1.4.4
aiosmtplib>=2.0.2

# Load settings from .env file
python-dotenv>=1.0.0

# OAuth2 / Microsoft Graph API (required when AUTH_MODE=oauth2_graph)
msal>=1.28.0
httpx>=0.27.0
